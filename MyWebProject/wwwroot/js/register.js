const FORM_SUBMIT = document.getElementById('form');
eventListeners();

function eventListeners() {
    FORM_SUBMIT.addEventListener('submit', formAlert);
    
}
function formAlert
    {
    alert("DF");
}